import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  image: {
    height: 200,
    justifyContent: 'center',
    resizeMode: 'cover',
  },
  sliderTitle: {
    marginTop: 5,
    fontWeight: 'bold',
    color: 'black',
    fontSize: 35,
  },
  sliderText: {
    marginVertical: 10,
    marginBottom: 30,
    fontWeight: '500',
    color: 'black',
    fontSize: 25,
  },
});
