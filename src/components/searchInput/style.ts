import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    margin: 10,
    padding: 5,
  },
  input: {
    fontSize: 20,
    borderWidth: 1,
    borderColor: 'grey',
    borderRadius: 5,
    padding: 10,
    backgroundColor: '#fff',
  },
});
